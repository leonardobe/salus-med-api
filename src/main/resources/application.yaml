spring:
  application:
    name: api

  datasource:
    url: jdbc:mysql://localhost:3306/vollmed_api
    username: root
    password: BombardaMysql@53
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
        use_sql_comments: true

  jackson:
    serialization:
      fail-on-empty-beans: false

springdoc:
  packages-to-scan: med.voll.api